extends layout

block content2
  .container
    .row
     .col-md-4
       h1.title Tasks Completed
         ul.list-group
           each task in tasks
             li.list-unstyled
               span.itemName #{task.title}